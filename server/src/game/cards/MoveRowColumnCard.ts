import type { Card } from "./Card";

export const moveRowColumnCard: Card = {
  id: "move-row-column",
  type: "MOVE_ROW_COLUMN",
  name: "Move Row / Column",
  description:
    "Move an entire row or column one step with wrap-around.",
};